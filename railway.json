{
    "build": {
        "builder": "nixpacks",
        "config": {
            "language": "php",
            "startCommand": "php -S 0.0.0.0:{PORT} -t public"
        }
    },
    "deploy":{
        "startCommand": "php -S 0.0.0.0:{PORT} -t public"
    }
}
